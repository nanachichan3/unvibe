'use client';

/**
 * Hero — simplified, upload zone moved to LandingUpload component.
 * Landing page is now composed in app/page.tsx.
 * This file is kept for reference and can be removed in a future cleanup.
 */
export default function Hero() {
  return null;
}
